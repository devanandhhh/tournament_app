import 'package:flutter/material.dart';

class SecondFixture extends StatelessWidget {
  const SecondFixture({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Second Screen')),appBar: AppBar(),);
  }
}