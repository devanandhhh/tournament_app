import 'package:flutter/material.dart';

class MatchScreenView extends StatelessWidget {
  const MatchScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Match  Screen')));
  }
}